# {{RUN_ID}}

One job: turn one approved idea into one release-ready carousel package.

Read `run.json` first. Use the workspace stage contract matching `status`. Keep exact copy in `copy.md`, visual decisions in `visual-brief.md`, generation records in `prompts.md`, and gate evidence in `qa.md`.
