# {{CLIENT_NAME}} carousel Pipeline

One job: produce governed social carousels for {{CLIENT_NAME}}.

## Route

| Need | Stage |
|---|---|
| Gather goals, evidence, brand, voice, people, platform route, and rights | `stages/01_intake/` |
| Build slide strategy and exact copy | `stages/02_strategy_copy/` |
| Generate and approve the cover anchor | `stages/03_anchor/` |
| Produce separate slides one at a time | `stages/04_production/` |
| Validate and package the release candidate | `stages/05_release/` |

Stable client rules live in `_shared/`. Guided setup lives in `setup/`. Blank run files live in `_templates/`. Each product run lives in `runs/`.

Owner: {{OWNER}}
Created: {{CREATED_DATE}}
